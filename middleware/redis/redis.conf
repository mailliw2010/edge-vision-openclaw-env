# OpenClaw middleware Redis config.
#
# This keeps Redis reachable from the Compose network while preserving the
# append-only persistence mode already used by the stack.

bind 0.0.0.0
port 6379
protected-mode no
daemonize no
dir /data
appendonly yes
save 900 1
save 300 10
save 60 10000
